<h1>Edit person</h1>

<%= form_for(@person) do |f| %>

  <fieldset>
    <%= render :partial => 'form', :locals => { :f => f } %>
    <br>

    <%= f.submit "Edit " + @person.first_name + " " + @person.last_name, class: "btn btn-large btn-primary" %>
  </fieldset>

<% end %>



